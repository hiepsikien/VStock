# Local Expo (`npx expo start` / npm start)
# On a physical device, client rewrites localhost → Metro LAN IP automatically.
# Uvicorn must bind 0.0.0.0:  uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
EXPO_PUBLIC_API_URL=http://localhost:8000
